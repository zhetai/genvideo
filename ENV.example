# Environment Configuration

Copy this file to `env.ts` for local development or configure environment variables for deployment.

# Environment Variables for Cloudflare Workers
## Required Variables:
# - DASHSCOPE_API_KEY: Your Aliyun DashScope API key (for video generation)
# - DEEPSEEK_API_KEY: Your DeepSeek API key (for LLM features) - Optional

# Example wrangler.toml configuration:
# [vars]
# DEEPSEEK_API_KEY = "your-deepseek-api-key-here"

# For local development, create an env.ts file with:
export const ENV = {
  DASHSCOPE_API_KEY: 'your-dashscope-api-key-here',
  DEEPSEEK_API_KEY: 'your-deepseek-api-key-here', // Optional
};

# DeepSeek API Key Setup
# 1. Get your API key from https://platform.deepseek.com/
# 2. Add it to your Cloudflare Worker environment variables
# 3. Or set DEEPSEEK_API_KEY in wrangler.toml for deployment
